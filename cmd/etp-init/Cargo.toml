[package]
name = "etp-init"
version = "0.2.0"
edition = "2024"

[dependencies]
etp-lib = { path = "../../crates/etp-lib" }
anyhow = "1"
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
trycmd = "1.0"
